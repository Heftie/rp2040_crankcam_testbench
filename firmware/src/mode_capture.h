// Mode 3: one-shot crank/cam generation + 6-channel ECU output capture +
// angle conversion + pass/fail evaluation.
#ifndef MODE_CAPTURE_H
#define MODE_CAPTURE_H

void run_mode_capture_test(void);

#endif
