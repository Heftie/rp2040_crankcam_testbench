// Mode 4: continuous one-shot gen + 6ch capture, one 720deg cycle at a
// time, reporting each channel's rise/fall angle after every cycle
// (no pass/fail tolerance -- just what was detected).
#ifndef MODE_LIVE_H
#define MODE_LIVE_H

void run_mode_live(void);

#endif
